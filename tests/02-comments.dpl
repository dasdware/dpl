# This is a comment. It should be ignored.
print(123);

# There are no multiline comments.
# Every line needs to start with its own `#` character.

print(456); # This should be ignored until the end of the line

# Trailing comment at end of file